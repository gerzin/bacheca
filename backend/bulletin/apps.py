"""App configuration for the bulletin app."""

from django.apps import AppConfig


class BulletinConfig(AppConfig):
    """Configuration for the bulletin app."""

    name = "bulletin"
    verbose_name = "Bulletin Board"
